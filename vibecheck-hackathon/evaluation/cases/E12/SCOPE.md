# Project Scope

## [REQ-01] Protected Account Data

- Account data must only be accessible to authenticated users
- Unauthenticated requests must be rejected
- The authentication guard must execute before account data is returned
